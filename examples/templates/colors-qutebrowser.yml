colors.statusbar.caret.bg: "{{ colors.color5 }}"
colors.statusbar.caret.fg: "{{ colors.foreground }}"

colors.statusbar.command.bg: "{{ colors.color6 }}"
colors.statusbar.command.fg: "{{ colors.background }}"
colors.statusbar.command.private.bg: "black"

colors.statusbar.insert.bg: "{{ colors.color2 }}"
colors.statusbar.insert.fg: "{{ colors.foreground }}"

colors.statusbar.normal.bg: "{{ colors.background }}"
colors.statusbar.normal.fg: "{{ colors.foreground }}"

colors.statusbar.private.bg: "{{ colors.background }}"
colors.statusbar.private.fg: "{{ colors.foreground }}"

colors.statusbar.progress.bg: "{{ colors.foreground }}"

colors.statusbar.url.fg: "{{ colors.foreground }}"
colors.statusbar.url.error.fg: "{{ colors.color1 }}"
colors.statusbar.url.hover.fg: "{{ colors.color4 }}"
colors.statusbar.url.success.http.fg: "{{ colors.foreground }}"
colors.statusbar.url.success.https.fg: "{{ colors.color2 }}"
colors.statusbar.url.warn.fg: "{{ colors.color3 }}"

colors.tabs.bar.bg: "{{ colors.background }}"
colors.tabs.even.fg: "{{ colors.foreground }}"
colors.tabs.even.bg: "{{ colors.background }}"
colors.tabs.odd.fg: "{{ colors.foreground }}"
colors.tabs.odd.bg: "{{ colors.background }}"

colors.tabs.selected.even.fg: "{{ colors.background }}"
colors.tabs.selected.even.bg: "{{ colors.foreground }}"

colors.tabs.selected.odd.fg: "{{ colors.background }}"
colors.tabs.selected.odd.bg: "{{ colors.foreground }}"

colors.completion.category.bg: "{{ colors.color6 }}"
colors.completion.category.fg: "{{ colors.background }}"

colors.completion.category.border.bottom: "{{ colors.background }}"
colors.completion.category.border.top: "{{ colors.color6 }}"

colors.completion.even.bg: "{{ colors.background }}"
colors.completion.odd.bg: "{{ colors.background }}"

colors.completion.item.selected.bg: "{{ colors.color4 }}"
colors.completion.item.selected.fg: "{{ colors.foreground }}"

colors.completion.item.selected.border.bottom: "{{ colors.color4 }}"
colors.completion.item.selected.border.top: "{{ colors.color4 }}"

colors.completion.match.fg: "{{ colors.color2 }}"

colors.completion.scrollbar.bg: "{{ colors.foreground }}"
colors.completion.scrollbar.fg: "{{ colors.background }}"

colors.downloads.bar.bg: "{{ colors.background }}"
colors.downloads.error.fg: "{{ colors.foreground }}"
colors.downloads.error.bg: "{{ colors.color1 }}"
colors.downloads.start.fg: "{{ colors.background }}"
colors.downloads.start.bg: "{{ colors.color5 }}"
colors.downloads.stop.fg: "{{ colors.background }}"
colors.downloads.stop.bg: "{{ colors.color2 }}"

colors.hints.fg: "{{ colors.foreground }}"
colors.hints.bg: "{{ colors.background }}"
colors.hints.match.fg: "{{ colors.color3 }}"

colors.keyhint.fg: "{{ colors.foreground }}"
colors.keyhint.bg: "{{ colors.background }}"
colors.keyhint.suffix.fg: "{{ colors.color3 }}"

colors.messages.error.fg: "{{ colors.foreground }}"
colors.messages.error.bg: "{{ colors.color1 }}"
colors.messages.error.border: "{{ colors.color1 }}"

colors.messages.info.fg: "{{ colors.foreground }}"
colors.messages.info.bg: "{{ colors.color2 }}"
colors.messages.info.border: "{{ colors.color2 }}"

colors.messages.warning.fg: "{{ colors.foreground }}"
colors.messages.warning.bg: "{{ colors.color3 }}"
colors.messages.warning.border: "{{ colors.color3 }}"

colors.prompts.fg: "{{ colors.foreground }}"
colors.prompts.bg: "{{ colors.background }}"
colors.prompts.border: "{{ colors.background }}"
colors.prompts.selected.bg: "{{ colors.color3 }}"

hints.border: "1px solid {{ colors.foreground }}"
